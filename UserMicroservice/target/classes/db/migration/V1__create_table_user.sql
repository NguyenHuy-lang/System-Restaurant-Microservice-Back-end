CREATE TABLE tbl_user (
    ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    address varchar(100),
    name varchar(100),
    password varchar(200),
    phone varchar(50),
    role varchar(50),
    username varchar(50),
    email varchar(50)
)
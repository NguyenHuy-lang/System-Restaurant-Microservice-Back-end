CREATE TABLE tbl_food(
                         ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
                         name varchar(100),
                         price varchar(100),
                         type varchar(50),
                         status varchar(50)
)